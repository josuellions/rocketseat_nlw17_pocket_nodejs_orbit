{
  "name": "orbit",
  "version": "1.0.0",
  "description": "Desenvolvimento acadêmico de uma aplicação back-end em Node.js, conceitos de API REST, gerenciamento metas pessoais diária de atividades e bem-estar com interação.",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch --env-file .env src/http/server.ts",
    "services:up": "docker compose up -d",
    "services:down": "docker compose down",
    "services:db:migrate:test": "dotenv -e .env.test -- drizzle-kit migrate",
    "services:db:generate": "npx drizzle-kit generate",
    "services:db:migrate": "npx drizzle-kit migrate",
    "services:db:studio": "npx drizzle-kit studio",
    "services:db:seed": "tsx --env-file .env src/db/seed.ts",
    "services:format": "biome format src/ --write",
    "pretest": "npm run services:db:migrate:test",
    "pretest:watch": "npm run services:db:migrate:test",
    "test:watch": "dotenv -e .env.test -- vitest",
    "test": "dotenv -e .env.test -- vitest run"
  },
  "author": "Josuel A. Lopes",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^22.10.6",
    "dotenv-cli": "^8.0.0",
    "drizzle-kit": "^0.30.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "-": "^0.0.1",
    "@faker-js/faker": "^9.4.0",
    "@fastify/cors": "^10.0.2",
    "@fastify/jwt": "^9.0.3",
    "@fastify/swagger": "^9.4.1",
    "@fastify/swagger-ui": "^5.2.1",
    "@paralleldrive/cuid2": "^2.2.2",
    "D": "^1.0.0",
    "dayjs": "^1.11.13",
    "drizzle-orm": "^0.38.3",
    "fastify": "^5.2.1",
    "fastify-type-provider-zod": "^4.0.2",
    "jose": "^5.9.6",
    "postgres": "^3.4.5",
    "zod": "^3.24.1"
  }
}
